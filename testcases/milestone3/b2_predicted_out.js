function sliceMe() {
    var str = "sample statement";
    var letter = "s"
    var length = str.length;
    // looping through the items
    for (var i = 0; i < length; i++) {
        str.charAt(i) == letter;
    }
}

sliceMe();