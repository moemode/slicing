function sliceMe() {    
    var str = "sample statement";
    var letter = "s"
    var length = str.length;
    for (var i = 0; i < length; i++) {
        if (str.charAt(i) == letter) {
        }
    }
}

sliceMe();